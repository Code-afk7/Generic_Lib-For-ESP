Wifi Based Smart Config it will dynamically Connect your Your With Wifi using ESP_Touch. 
use App like ESP_SmartConfig to connect the Wifi without Hard Coding the SSID and Password.

If the SSID and PassWord is Connected Once then it will Store the value in NVM[ Non - Volatile Memory ]. 
  the next time device is turned on then the wifi will directly be connected.
  the new ssid and password is also stored in the NVM.

Better way to connect your wifi with ESP.

It also supports Diffrent mode like ESP-touch V2 and Air-kiss.

                                                        -Sher 
